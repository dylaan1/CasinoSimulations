"""termjack: a small terminal blackjack game.

Standalone from the ``blackjack`` simulator package -- it borrows the same
core concepts (cards, shoe, hand values, dealer logic) but is built around
an interactive, human-driven round instead of a strategy-table simulation.
"""

__version__ = "0.1.0"
